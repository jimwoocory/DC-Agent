# Codex 修复任务清单 — 2026-05-20 改造审核反馈

> **生成时间**：2026-05-20 13:50（北京）
> **审核工具**：Claude Code（5 个并行审核 Agent 分模块审查）
> **改造规模**：48 文件、+1959 / -482 行、112 patch，**全部未 commit，都在工作区**
> **执行者**：Codex CLI

---

## ⚠️ 工作原则（请先读）

1. **🔴 必修任务全部完成前，禁止 `git add` / `git commit`**。原因：今天 `nas_sync/config.yaml` 工作区里有明文密码 + `dc_router_config.json` 把生产开关 `enabled=true` 也带在工作区，一旦提交立刻泄漏 / 失控。
2. **每修一项前先 `git diff -- <file>` 确认问题仍然存在**（避免审核结果与当前状态脱节）。
3. **每修完一项**：
   - 跑该模块相关的测试（命令见每项末尾）
   - 在本文件对应 checkbox 打勾 `[x]` + 写上 commit hash 占位（暂不真 commit，先做完所有再统一）
4. **🟡 / 🔵 任务允许在 🔴 完成 + 第一轮 commit 后再做**，分独立 commit。
5. **不允许"顺手重构"**：每项任务只做描述里的事，超出范围的代码味道发现了请追加到本文件末尾「附录：发现的额外问题」，不要自行修。
6. **凭证处理特别注意**：本文件全程禁止把任何密码 / token / secret 字面值写进新代码或注释。

---

## 🔴 必修任务（commit 之前必须完成）

### Task R-1：凭证脱敏 ⚠️ P0 BLOCKING

**问题**：
- `nas_sync/config.yaml:20-21` 明文 NAS 密码 `[REDACTED_NAS_PASSWORD]`
- `nas_sync/config.yaml:97-99` 明文 Dashboard 密码 `[REDACTED_DASHBOARD_PASSWORD]`
- `nas_sync/mount.sh:78` 把密码拼到 `mount_smbfs //user:pass@host/share` 命令行（任何 `ps -ef` 可见）
- `data/plugins/hermes_escalation_plugin/main.py:95` `secret = bridge.get("secret", "${HERMES_BRIDGE_SECRET}")` ← 把生产 secret 字面量写在 fallback 里
- `data/plugins/hermes_bridge/hermes_bridge.py:194-196` 同样的 secret fallback 问题

**修复要求**：
1. 把 `config.yaml` 里两组密码迁出，方案择一（推荐第一种）：
   - **方案 A**：迁到 macOS Keychain，`watcher.py` / `mount.sh` 用 `security find-generic-password -s <key> -w` 读
   - **方案 B**：迁到 `~/.config/dc-agent/secrets.env`，文件权限 `0600`，`config.yaml` 改成读环境变量
2. `mount.sh` 改成密码 via stdin 传给 `mount_smbfs` 或 Keychain 路径，**禁止**留在命令行参数里
3. `hermes_escalation_plugin/main.py:95` 和 `hermes_bridge/hermes_bridge.py:194-196` 的 secret fallback：
   - 配置文件存在 → 用配置
   - 配置文件不存在 → `raise RuntimeError("hermes bridge secret 未配置，请补 data/config/hermes_bridge_config.json")`，**禁止**硬编码 fallback 字面值
4. `config.yaml` 改完后内容里只能有 `username: dcwh-001` + 一个 `password_keychain_key: dc-agent-nas-password`（或类似 reference）
5. **检查 `.gitignore`**：确保 `nas_sync/config.yaml` **如果还含敏感字段** 加入 `.gitignore`；如果已经脱敏成 reference-only，可以正常 commit

**验证**：
```bash
cd /Users/dianchi/DC-Agent
# 工作区里不应该再有明文密码
grep -rE "[REDACTED_NAS_PASSWORD]|[REDACTED_DASHBOARD_PASSWORD]|${HERMES_BRIDGE_SECRET}" --include='*.py' --include='*.yaml' --include='*.json' --include='*.sh'
# 应该返回空
```

**完成标记**：- [x] R-1 done（commit: pending）

---

### Task R-2：dc_router_config 回到灰度状态

**问题**：[`data/config/dc_router_config.json:1-4`](data/config/dc_router_config.json)
```json
{ "enabled": true, "dry_run": false, "fallback_on_error": true }
```
当前工作区的状态是"直接正式接管路由"。README 还在写"灰度建议先 dry_run=true"。同时 [`test_dc_router_path.py:38-47`](data/plugins/llm_router/test_dc_router_path.py:38) 把这个状态固化进 `test_config_current_state_active`。

**修复要求**：
1. `dc_router_config.json` 改回 `enabled: false, dry_run: true`，让 commit 推上去是安全的"默认关闭"状态
2. 真要在生产开启时，由运维手动改这个文件（不进 git 历史）
3. `test_dc_router_path.py:38-47` 的测试改成"断言 enabled / dry_run 字段存在且是 bool"，而不是断言具体值
4. 如果你今天确实需要在本机继续测试 `enabled=true`，把这个改动做成本地 git stash，**不要进 commit**

**验证**：
```bash
cd /Users/dianchi/DC-Agent
cat data/config/dc_router_config.json | python3 -c 'import json,sys; c=json.load(sys.stdin); assert c["enabled"]==False and c["dry_run"]==True'
python -m pytest data/plugins/llm_router/test_dc_router_path.py::test_config_current_state_active -v
```

**完成标记**：- [x] R-2 done（commit: pending）

---

### Task R-3：`truth_intake_guard` 在 `enabled=false` 时不应运行

**问题**：[`data/plugins/llm_router/main.py:710-728`](data/plugins/llm_router/main.py:710)
```python
_dc_cfg = _read_dc_router_config()
...
if _should_enter_dc_router(platform_id):     # ← 只看平台白名单
    try:
        from truth_intake_guard import maybe_handle_truth_intake
        if await maybe_handle_truth_intake(self.context, event):
            return                            # ← enabled=false 时也会拦
```
README 承诺"`enabled=false` 系统行为跟原来 100% 一样"，但 guard 不看这个开关。

**修复要求**：
1. 在 `if _should_enter_dc_router(platform_id):` 这个条件里加 `and _dc_cfg.get("enabled", False)`
2. `dry_run=true` 时应该走完 guard 的逻辑（写 task / 判定）但**不要拦截 event**，让 v1.0 路径继续跑（影子模式）
3. 同步在 [`truth_intake_guard.py`](data/plugins/llm_router/truth_intake_guard.py) 入口处加一个 early-return：传入 cfg，`cfg.enabled=False` → 立即 `return False`
4. 把"engine 不可用"这类宽 except 路径（line 104, 111, 178, 193, 462, 466, 496, 514）改成：engine 不可用 → `return False`，**绝对不要拦截消息后又装作没事**

**验证**：补一个测试 case：
```python
# test_dc_router_path.py 新增
async def test_truth_intake_guard_respects_enabled_off():
    # 1) 把 dc_router_config 临时改成 enabled=false
    # 2) 发一条真实文案 intake 触发消息
    # 3) 断言 maybe_handle_truth_intake 返回 False
    # 4) 断言 v1.0 链路正常处理了这条消息
```

**完成标记**：- [x] R-3 done（commit: pending）

---

### Task R-4：Hermes Bridge dedup 锁与"短文本中间态"判定冲突

**问题**：[`data/plugins/hermes_bridge/hermes_bridge.py:640-655`](data/plugins/hermes_bridge/hermes_bridge.py:640) 外层：
```python
if card_handled:
    if task_id and not _re_search(
        r"Still working|iteration \d+/\d+|Retrying in",
        response_text,
    ):
        finalized.add(task_id)
```

但 [`hermes_bridge.py:762-766`](data/plugins/hermes_bridge/hermes_bridge.py:762) 内部：
```python
if not is_intermediate and not is_final_failure and len(response_text) < 500:
    is_intermediate = True       # 短文本 + 不含关键词 → 视为中间态
```

**冲突场景**：Hermes 推 < 500 字、不含 "Still working/iteration/Retrying" 关键词的中间摘要 →
- 内部：`is_intermediate=True`，不 finalize（return True）
- 外层：正则匹配不到，**错误地 `finalized.add(task_id)`**
- 真正最终结果到达 → dedup 命中 → 返回 `dedup_skip`
- **用户感知**：飞书卡片"卡在加载，永远不出最终结果"

**修复要求**：
1. 让 `_handle_response_via_card` 返回值变成结构化结果（dataclass / dict），至少包含 `is_finalized: bool` 字段
2. 外层（line 640-655）**只**用这个 `is_finalized` 字段决定 dedup，不要再跑正则
3. 同时 `_handle_response_via_card` 最终成功分支（line 830-832）`streamer.finalize` 失败时：
   - **不要** `stream_map.pop(task_id)`
   - **不要** 让外层 dedup
   - 返回 `is_finalized=False` 让上游能重发或落 DLQ

**验证**：补测试 case：
```python
# tests/harness/test_hermes_bridge.py 新增
async def test_short_intermediate_followed_by_final():
    # 1) Hermes 先推一条 200 字、不含 "Still working" 关键词的中间摘要
    # 2) Hermes 接着推 3000 字最终结果
    # 3) 断言 streamer.finalize 被调一次（最终结果）
    # 4) 断言用户在飞书看得到最终卡片（不是卡在加载状态）

async def test_finalize_failure_does_not_dedup():
    # 1) Mock streamer.finalize 抛异常
    # 2) 触发最终成功路径
    # 3) 断言 task_id 不在 _finalized_task_ids 里
    # 4) 断言 stream_map 还有该 task_id（可重发）
```

**完成标记**：- [x] R-4 done（commit: pending）

---

### Task R-5：员工入职插件三个维护期开关失守

**问题**：用户明确说"系统还在维护中，不要直接运行自动巡检"。当前定时巡检关了（`active_outreach_enabled=False`），但被动触发面全是开的。

**三个失守点**：

**a) `on_lark_private` 无总开关** — [`employee_onboarding/main.py:410-468`](data/plugins/employee_onboarding/main.py:410)
员工主动私聊"在吗" → 立即进入 `_start_onboarding` 推部门卡。

**b) Admin 命令无 maintenance gate** — [`employee_onboarding/main.py:825-836`](data/plugins/employee_onboarding/main.py:825)
```python
if sub in ("scan", "remind"):
    report = await self._run_outreach_scan(force=(sub == "remind"))
```
`/onboarding remind` 还 force=True 忽略冷却 → **一次误操作给全公司未完成员工群发卡片**。

**c) `__card_action__:` 缺来源校验** — [`employee_onboarding/main.py:425-428`](data/plugins/employee_onboarding/main.py:425)
员工只要发字符串 `__card_action__:{"value":{"action":"submit_quiz","q_num":5,"choice":"B"}}` 就能直接塞答案、绕过 quiz、最终触发 `_invite_after_pass`（如果开了 `auto_invite_to_chat=True`）+ 让自己 `stage` 跳到 `passed/invited`。
另外：`/重新入职 / /onboard / /reonboard` 没有 admin 权限保护。

**修复要求**：
1. 在 `_conf_schema.json` + `employee_onboarding_config.json` 新增两个顶层开关，默认 `false`：
   - `enabled` (bool, default false)：整插件总开关
   - `maintenance_mode` (bool, default true) 或者直接用 `enabled=false` 替代
2. `on_lark_private`、`_handle_card_action`、`_run_outreach_scan`、管理员命令入口 — 每个入口最开始加 `if not self.enabled or self.maintenance_mode: return`
3. `__card_action__:` 前缀做来源校验：
   - 拿到 event 时检查这是不是真的飞书卡片回调（看 `event` 是否有 `is_card_action=True` 或类似的可信标志，看 [`lark_adapter.py:618-688`](astrbot/core/platform/sources/lark/lark_adapter.py:618) `convert_card_action` 入口怎么标记）
   - 文本前缀但没有可信标志 → 拒绝处理 + log warning
4. `/重新入职` / `/onboarding` 命令加 admin 权限检查（按 astrbot 现有权限模式）
5. [`employee_onboarding_config.json:11`](data/config/employee_onboarding_config.json:11) `invite_link` 当前是占位 URL `https://o0ain5w98jh.feishu.cn/q/...（待补真实链接）`。`build_quiz_result_card` 渲染时检测到包含 `…` 或 `（待补` 字样 → 跳过 invite button，改成纯文本"请联系管理员邀请进群"
6. 留个紧急关闭办法：环境变量 `EMPLOYEE_ONBOARDING_FORCE_DISABLE=1` 时无视配置直接 disable

**验证**：
```python
# 新建测试
async def test_onboarding_disabled_by_default_blocks_private():
    # 1) enabled=false（默认）
    # 2) 模拟员工私聊
    # 3) 断言 on_lark_private 直接 return，没调 _start_onboarding

async def test_card_action_requires_trusted_source():
    # 1) 直接发文本 __card_action__:{"value":...}
    # 2) 断言被拒绝处理 + log warning

async def test_admin_command_requires_permission():
    # 普通用户调 /onboarding scan → 拒绝
```

**完成标记**：- [x] R-5 done（commit: pending）

---

## 🟡 需要关注（commit 后这周内修，独立 commit）

### Task A-1：Lark 群卡片回调上下文丢失
[`astrbot/core/platform/sources/lark/lark_adapter.py:677`](astrbot/core/platform/sources/lark/lark_adapter.py:677)
```python
abm.type = MessageType.FRIEND_MESSAGE   # 群消息也被强制设为私聊
```
**修复**：拿 `ctx.open_chat_id`（飞书 chat 上下文），有就是 `GROUP_MESSAGE`，没有才是 `FRIEND_MESSAGE`。
**完成标记**：- [ ] A-1 done

### Task A-2：Lark DEBUG 日志泄漏 PII
[`lark_adapter.py:516-528`](astrbot/core/platform/sources/lark/lark_adapter.py:516) 注释自己写"临时调试"，INFO 级别打印 `open_id`。
**修复**：把 `logger.info` 改成 `logger.debug`，或彻底删掉这段调试日志（如果调试任务已完成）。
**完成标记**：- [ ] A-2 done

### Task A-3：Gemini OAuth 凭证非原子写入
[`astrbot/core/provider/sources/gemini_oauth_source.py:151`](astrbot/core/provider/sources/gemini_oauth_source.py:151)
```python
CREDENTIALS_PATH.write_text(json.dumps(creds, indent=2), encoding="utf-8")
```
**修复**：用 `tmp.write_text(data) + os.replace(tmp, target)` 模式；加 `fcntl.flock` 文件锁防止 Gemini CLI + AstrBot 并发 refresh 互相覆盖。同时把 `CREDENTIALS_PATH` 的绝对路径 `/Users/dianchi/DC-Agent/...` 改成相对当前文件的路径或环境变量。
**完成标记**：- [ ] A-3 done

### Task A-4：Codex OAuth 缺 401 → 主动刷新
[`codex_oauth_source.py:325-328`](astrbot/core/provider/sources/codex_oauth_source.py:325) 只在每次调用前读最新 token，没有"调用失败 401 → 再读一次 / 触发 codex CLI refresh"的处理。
**修复**：在 chat completion 失败 401 时调 `codex login --refresh` 或重读 `auth.json` 一次再重试。
**完成标记**：- [ ] A-4 done

### Task A-5：NAS 去重与 KB 状态联动
[`nas_sync/dedupe_admin.py`](nas_sync/dedupe_admin.py) quarantine 后不更新 `state.json`，下次 `scan_inbox` 把"幸存副本"按新 rel-path 重新摄入到 KB。
**修复**：
1. quarantine 时调 `IngestState.remove(rel)` 把被搬走的文件从 state.json 删掉
2. 在 [`nas_sync/watcher.py`](nas_sync/watcher.py) `state.is_ingested(rel, file_hash)` 改成 **hash-first** 索引（按内容哈希查，命中就跳过，不绑死 rel-path）
**完成标记**：- [ ] A-5 done

### Task A-6：dedupe_admin 跨子树 move 在 SMB 上撕碎正在写入的文件
[`nas_sync/dedupe_admin.py:248`](nas_sync/dedupe_admin.py:248) `shutil.move` 在跨子树 / SMB 上会 fallback 到 copy+unlink。
**修复**：
1. 改用 `os.rename`，rename 失败直接 skip 该条 + 写 manifest 记录 skip 原因，不要 fallback copy
2. 加 lock：调用前先 acquire 与 `sync_baidu_to_nas.sh` / `feishu_sync.py` 共享的 lock（建议放 `~/nas_kb/.dc-locks/dedupe.lock`）
3. 默认拒绝隔离最近 N 分钟（默认 10 分钟）内 mtime 变过的文件，避免动正在写的
**完成标记**：- [ ] A-6 done

### Task A-7：dedupe_admin 硬链接误判
[`nas_sync/dedupe_admin.py:131-202`](nas_sync/dedupe_admin.py:131) `build_duplicate_report` 只按 `(size, sha256)` 分组，没读 `st_ino/st_dev`。
**修复**：分组前用 `(st_dev, st_ino)` 去重，inode 相同就跳过（同物理数据的硬链接当作同一个文件，不进 quarantine）。
**完成标记**：- [ ] A-7 done

### Task A-8：knowledge_cycle daily_full 暴露面过大
[`scripts-watchdog/knowledge_cycle.py:209-217`](scripts-watchdog/knowledge_cycle.py:209) 仅靠环境变量 `KNOWLEDGE_ENABLE_DAILY_FULL_SCAN=1` 就能触发全量扫描。state.json 当前 12 条都是 inbox 短名，一旦切全量扫描 100% 触发二次摄入。
**修复**：
1. 加双重确认：必须 `KNOWLEDGE_ENABLE_DAILY_FULL_SCAN=1` **且** 存在 `data/watchdog/ALLOW_FULL_SCAN.flag` 文件
2. 前置做完 Task A-5 的 hash-first 索引（不然不能安全开 full scan）
**完成标记**：- [ ] A-8 done

### Task A-9：quota_gate.fail() 防御 PENDING 误调
[`harness/quota_gate.py:240-279`](harness/quota_gate.py:240) `fail()` 没有过滤 status，对 PENDING 调用会污染其他 RUNNING 的 in_flight_job_id。
**修复**：`fail()` 开头加 `row = self._fetch_job(job_id); if row["status"] == PENDING: return self.cancel_pending_job(job_id, reason=str(error))` 或者 raise ValueError 让调用方知道用错 API。
**完成标记**：- [ ] A-9 done

### Task A-10：feishu_sync `_target_for` 运算符优先级 bug
[`nas_sync/feishu_sync.py:294`](nas_sync/feishu_sync.py:294)
```python
if str(target) == prev_local or str(target) not in claimed_paths and not target.exists():
```
Python 解析为 `A or (B and C)`，导致 `prev_local != target` 且 `target 不存在` 且 `claimed 包含 target` 时仍返回 target，绕过 claimed 检查。
**修复**：加括号 → `(str(target) == prev_local) or (str(target) not in claimed_paths and not target.exists())`
**完成标记**：- [ ] A-10 done

### Task A-11：飞书 URL inline 没数量上限
[`data/plugins/llm_router/dc_router_adapter.py:946-987`](data/plugins/llm_router/dc_router_adapter.py:946) 每个 URL 截到 8000 字符，但没限数量。10 个链接就 80000 字符爆 context。
**修复**：加 `MAX_FEISHU_DOCS_PER_MESSAGE = 3`，超过的 URL 替换成"（更多飞书链接已省略，请逐条提问）"提示。
**完成标记**：- [ ] A-11 done

### Task A-12：失败诊断 codex_cli_failure_report 串行 await 90s
[`dc_router_adapter.py:1088-1104`](data/plugins/llm_router/dc_router_adapter.py:1088) 主任务异常（可能 Claude CLI 僵死 900s）后串行 await 新的 90s codex 诊断。员工要等 990+s 才看到错误回卡。
**修复**：
1. 先把 fallback 错误卡发出去（用户知道失败了）
2. 再异步触发诊断（`asyncio.create_task` + 保留引用），完成后用 follow-up message 追加
3. `_codex_cli_failure_report` 硬超时降到 30s
**完成标记**：- [ ] A-12 done

### Task A-13：daily_card_renderer 3 秒 patch 频率优化
[`data/plugins/daily_card_renderer/main.py:136`](data/plugins/daily_card_renderer/main.py:136) `interval_sec=3.0`，100 并发用户每秒 33 个 patch。
**修复**：
1. 在 `start_auto_update` builder 里比较 last_card_payload 与新 card，相同则跳过 patch
2. 或者把固定 3s 改成自适应（3s, 5s, 10s, 20s 退避）
**完成标记**：- [ ] A-13 done

### Task A-14：daily_card_renderer 旁路 finalize 内存泄漏
[`data/plugins/daily_card_renderer/main.py:254-268`](data/plugins/daily_card_renderer/main.py:254) 直接设 `stream.finalized=True`，绕过 `streamer.finalize` → `_streams` dict 只进不出。
**修复**：改成调 `streamer.finalize(message_id, final_card)`（或专门加一个 `streamer.finalize_silent(message_id)` 不发卡片但清 dict）。
**完成标记**：- [ ] A-14 done

### Task A-15：hermes_escalation_plugin 绝对路径
[`data/plugins/hermes_escalation_plugin/main.py:65-67`](data/plugins/hermes_escalation_plugin/main.py:65)
```python
cfg_path = Path("/Users/dianchi/DC-Agent/data/config/hermes_bridge_config.json")
```
**修复**：参照同模块 `hermes_bridge.py:218` 的 `Path(__file__).resolve().parents[3] / "data"` 模式。
**完成标记**：- [ ] A-15 done

---

## 🔵 改进 / 清理（独立 commit，有空再做）

- [ ] B-1：删除 `provider_map.py:15-16` 的 `HERMES_CLAUDE_SONNET/OPUS` 死代码
- [ ] B-2：[`README.md`](data/plugins/llm_router/README.md) 文件清单（line 76-83）补上 `truth_intake_guard.py`
- [ ] B-3：`cli_runner.py` `run_gemini` / `ALLOWED_BINS` 里的 `gemini` 字面值彻底删掉（或在 README 明示"保留 method 仅历史 db 兼容"）
- [ ] B-4：grep `data/personas/` `data/cmd_config.json` 看有没有写死 `dreamina_generate_image`（已被改名为 `dreamina_generate_image_fallback`）
- [ ] B-5：删除 `dc_engines/dc_engines/feishu_card_streamer/templates.py.bak`（35 KB，5 次改动应该 git 历史追）
- [ ] B-6：`concierge_plugin/main.py:155` `_CONCIERGE_PLATFORMS = {"巅池-Agent小助手"}` 迁到 `_conf_schema.json`
- [ ] B-7：`employee_onboarding/main.py:52` `LARK_PLATFORMS` 常量 + `target_platform_id` 配置项双源 → 只保留 config
- [ ] B-8：`i18n.js` 补全 `es`/`de` locale 段（行 803-916 大量条目仍是英文）
- [ ] B-9：JS 文件汉化漏改清扫（[`messages.js:371,379,494,498,724`](hermes-webui/static/messages.js)、[`sessions.js:233,258,506,713,884+`](hermes-webui/static/sessions.js)、[`ui.js:852,1323`](hermes-webui/static/ui.js)）
- [ ] B-10：补 `tests/test_feishu_card_templates.py` 缺失 case（空输入、长截断、缺字段、quiz passed/failed 双路、tutorial lesson_id 不存在）
- [ ] B-11：`hermes_bridge.py:629-632` `_finalized_task_ids` set 改成 `cachetools.TTLCache(maxsize=10000, ttl=86400)`
- [ ] B-12：`dedupe_admin.py` 默认 dry-run 时打印"以下文件将被搬走"预演 diff（限 50 条 + 总数）
- [ ] B-13：`sync_baidu_to_nas.sh` 顶部加 `set -euo pipefail` + `(( var++ ))` 改成 `((var+=1))`
- [ ] B-14：`scripts-watchdog/knowledge_cycle.py` 各 step 加 exponential backoff（失败时不要按 fixed interval 立即重试）
- [ ] B-15：`scripts-watchdog/diagnose.sh` incident id 用秒戳容易碰撞 → 改成 `nanosec` 或加 `$RANDOM`
- [ ] B-16：清理"历史 gemini pending job 数据库泥沙"（[`test_dc_router_path.py:1593-1648`](data/plugins/llm_router/test_dc_router_path.py:1593) 验证 skip 了但 db 里仍 PENDING）→ 写迁移脚本把 `backend="gemini"` 的 PENDING 标 cancelled 并回传通知

---

## 完成后整体验证

### 1) 测试套件全部 pass
```bash
cd /Users/dianchi/DC-Agent
python -m pytest data/plugins/llm_router/test_dc_router_path.py -v
python -m pytest tests/harness/test_hermes_bridge.py -v
python -m pytest tests/test_feishu_card_templates.py -v
python -m pytest tests/test_dashboard_quick_entries.py -v
```

### 2) 全局凭证扫描
```bash
cd /Users/dianchi/DC-Agent
grep -rE "[REDACTED_NAS_PASSWORD]|[REDACTED_DASHBOARD_PASSWORD]|${HERMES_BRIDGE_SECRET}|GOCSPX_REDACTED-" \
  --include='*.py' --include='*.yaml' --include='*.json' --include='*.sh' \
  --exclude-dir='data/_backup_*' --exclude-dir='.git'
# 应该返回空
```

### 3) 维护期开关验证
```bash
# 模拟员工私聊（需要先把 enabled=false）
cat data/config/employee_onboarding_config.json | python3 -c \
  'import json,sys; c=json.load(sys.stdin); assert c.get("enabled") in (False, None)'
```

### 4) dc_router_config 灰度状态
```bash
cat data/config/dc_router_config.json | python3 -c \
  'import json,sys; c=json.load(sys.stdin); assert c["enabled"]==False and c["dry_run"]==True'
```

### 5) 分组 commit
所有🔴修复完 → 第一组 commit（建议拆 5 个，每个对应一个 Task R-x）；
然后再做 🟡，每个独立 commit；
最后 🔵 一起 commit。

**禁止单一巨型 commit 把所有改动一次性合并**。

---

## 附录：发现的额外问题（修复过程中追加）

> Codex 在修复过程中如果发现本文档之外的问题，在这里追加，不要自行修。
> 格式：`- [文件:行号] 问题描述 — 发现时间`

（空）

---

## 上下文资料

- 完整审核报告：见 Claude Code session（2026-05-20 13:30）
- 今天的 Codex session 集中在 `/Users/dianchi/.codex/sessions/2026/05/20/`
- 自动备份文件在 `/Users/dianchi/DC-Agent/data/_backup_*`
- 5h 配额采样（2026-05-20 13:21）：primary 90% used，13:58 重置；secondary 15% used，5/27 03:02 重置
- 当前 `model_reasoning_effort` config 写的是 `xhigh`，UI 实际勾的是 `Max`（5 档最高）— 建议日常工作降到 `Medium`，复杂任务再切 `High`

---

**任务负责人**：Codex CLI
**审核生成方**：Claude Code (Opus 4.7)
**接收人**：dianchi
