# 运维排障记录

记录已排查和修复的生产问题，供后续参考。

---

## 2026-04-23 | QQ Bot 回复"OpenClaw 服务未运行"

### 现象
员工向 QQ Bot 发消息，Bot 回复：
> ⚠️ [系统提示] 无法完成指令。状态：请检查本地 OpenClaw Core 服务是否正常运行（端口 18789）...

### 根本原因
两个独立问题叠加：

**问题 1：Persona 的 custom_error_message 残留旧内容**
`data/data_v4.db` 的 `personas` 表中，3 个 Persona 的 `custom_error_message` 字段存有 OpenClaw 相关错误提示。当 LLM pipeline 抛出任何异常时，AstrBot 会返回该字段内容而非通用错误。

修复：
```sql
UPDATE personas
SET custom_error_message = '抱歉，处理您的请求时遇到了问题，请稍后再试。'
WHERE id IN (1, 2, 3);
```

**问题 2：会话历史损坏（tool role 孤儿消息）**
LLM 请求时报 `400 BadRequestError`：
> messages with role 'tool' must be a response to a preceeding message with 'tool_calls'

会话历史（264 条消息，99202 tokens）中存在孤儿 tool 消息，lossless compressor 也因此无法压缩，形成死循环。

修复（清空损坏会话）：
```sql
-- data/data_v4.db
UPDATE conversations
SET content = '[]', token_usage = 0
WHERE inner_conversation_id = 7;  -- 对应 conversation_id: fed66aa6-...

-- data/lossless_context.db
DELETE FROM lossless_items WHERE conversation_id = 'fed66aa6-7e41-491f-bcc2-d6dd2e920a39';
DELETE FROM lossless_heads WHERE conversation_id = 'fed66aa6-7e41-491f-bcc2-d6dd2e920a39';
```

### 经验
- persona `custom_error_message` 会在 LLM 任何异常时返回，要保持通用措辞
- 会话历史损坏后，lossless 压缩和正常请求都会失败；清空 content 是最快修复手段
- 清空后需同步清除 `lossless_context.db` 中的对应记录

---

## 2026-04-23 | QQ Bot 回复成功但用户收不到消息（Connection reset）

### 现象
AstrBot 日志显示 LLM 生成了完整回复，但出现：
```
ClientConnectorError: Cannot connect to host api.sgroup.qq.com:443
  → Connection reset by peer
```

### 根本原因
路由器（192.168.8.1）上的 Surge TUN 将 `api.sgroup.qq.com` 和 `multimedia.nt.qq.com.cn` 的流量走了不稳定的代理节点，偶发连接 reset。

确认特征：DNS 返回 `198.18.x.x`（Surge fake-IP 段），说明流量被 TUN 拦截。

修复：在路由器 Surge 规则中为 QQ API 域名添加 DIRECT 规则：
```
DOMAIN-SUFFIX,sgroup.qq.com,DIRECT
DOMAIN-SUFFIX,nt.qq.com.cn,DIRECT
```

### 经验
- `198.18.x.x` IP 段是 Surge fake-IP 的标志，出现即说明流量经过路由器 TUN
- 腾讯/QQ 域名直连更稳定，不需要走代理
- 飞书域名同理（open.feishu.cn），如出现 DNS 失败也考虑加 DIRECT

---

## 2026-04-23 | Dashboard Bot 卡片显示"1个错误"红色徽标

### 现象
AstrBot Dashboard 的 Bot 管理页面，4 个 Bot 卡片各显示红色"1个错误"。

### 根本原因
AstrBot 启动后立即尝试连接各平台 Websocket，此时 Surge TUN 尚未完全初始化，DNS 解析失败，连接报错。重试后成功，但错误计数已记录。

修复：在 `start-astrbot.sh` 启动脚本中加入 30 秒等待：
```bash
echo "[start-astrbot] Waiting 30s for Surge TUN to initialize..."
sleep 30
```

### 经验
- 错误徽标是累计计数，不是实时状态；连接成功后徽标不会自动清零
- 根本解法是给 TUN 足够的初始化时间，而非修改 AstrBot 代码

---

## 通用排障命令

### 查看实时日志
```bash
tail -f /Users/dianchi/DC-Agent/logs/launchd.out.log | grep -v "^\s*$"
```

### 查找错误
```bash
grep -E "ERRO|Exception|BadRequest|Connection reset" /Users/dianchi/DC-Agent/logs/launchd.out.log | tail -30
```

### 查看 Harness 任务状态
```bash
sqlite3 data/harness.db "SELECT task_id, status, title, updated_at FROM harness_tasks ORDER BY updated_at DESC LIMIT 10;"
```

### 查看最新记忆记录
```bash
sqlite3 data/harness_memory.db "SELECT session_id, substr(summary,1,80), created_at FROM harness_memories ORDER BY created_at DESC LIMIT 5;"
```

### 查看会话 token 用量
```bash
sqlite3 data/data_v4.db "SELECT inner_conversation_id, token_usage, user_id FROM conversations ORDER BY token_usage DESC LIMIT 5;"
```

### 检查 QQ API 连通性
```bash
curl -v --max-time 10 https://api.sgroup.qq.com 2>&1 | grep -E "Connected|reset|SSL|error"
# 198.18.x.x = Surge fake-IP（流量经过 TUN）
# 真实 IP = 直连
```

---

## 2026-05-12 | 三系统联合升级（Hermes / AstrBot / Dreamina CLI）

### 升级前后

| 系统 | 旧 | 新 | 备份位置 |
|---|---|---|---|
| Hermes Agent | v0.8.0 (2026-04-11, Python 3.11) | v0.13.0 / 2026.5.7 (Python 3.12) | `hermes-config/state.db.pre-v0.13-backup` |
| AstrBot | v4.22.3 | v4.24.2 | `data/backups/pre-v4.24.2-20260509_133424/` |
| Dreamina CLI | 1.3.4 (commit fa7ede2) | 1.4.3 (commit c58a6a2) | `~/.dreamina_cli/backup-pre-1.4.3-20260512_093112/` |

### 关键操作

1. **Hermes 升级**（无停机的方式）
   - 新装到 `/Users/dianchi/DC-Agent/hermes-agent/`（旧的 `hermes-agent-temp/` 保留）
   - `hermes-start.sh` 改 `HERMES_DIR` 指向新路径
   - `kill <pid>` + 启动新版（`--replace` 模式没在端口冲突时生效，要先杀旧的）
   - 配置目录 `hermes-config/` 不动，state.db schema 0.8 → 0.13 兼容

2. **AstrBot 升级**（fork merge）
   - upstream remote 修正：`Astronauts-Softworks/AstrBot.git` (404) → `AstrBotDevs/AstrBot.git`
   - `git merge v4.24.2` 产生 40 个冲突，其中：
     - 29 个 .py 全是 ruff format 噪音 → 全部 `--theirs`
     - 3 个 upstream 删除（persona.py / fs.py / shell.py）→ 接受删除
     - 7 个 changelog + 4 个 dashboard 字体 → 接受 upstream
     - 1 个 README → 保留本地（JW-Bot 重写版）
   - **git auto-merge 静默丢了 2 处本地代码**（事后修复 commit `76e84e774`）：
     - `MainAgentBuildConfig` 的 `lossless_context_enabled` + `lossless_compact_message_threshold` 字段
     - `core_lifecycle.py` 的整个 Harness sidecar 初始化块（44 行）+ `Context()` 调用的 harness 参数
   - 详见 [`DOC/本地功能契约.md`](本地功能契约.md)

3. **Dreamina CLI 升级**
   ```bash
   curl -s https://jimeng.jianying.com/cli | bash
   # 二进制：~/.local/bin/dreamina
   # 版本来源：~/.dreamina_cli/version.json
   # 最新版查询：curl https://lf3-static.bytednsdoc.com/obj/eden-cn/psj_hupthlyk/ljhwZthlaukjlkulzlp/version.json
   ```
   - 1.4.3 换了 sign_key_pair，旧 credential 失效，必须 `dreamina relogin`
   - relogin 流程：`dreamina relogin --headless` 拿 verification_uri → 浏览器扫码 → `dreamina login checklogin --device_code=<...> --poll=600` 收尾

### 启动管理

- AstrBot 通过 `~/Library/LaunchAgents/io.astrbot.bot.plist` 拉起 `start-all.sh`（含 main.py、Vue dashboard pnpm dev :4311、openclaw-control-center :4312）
- 重启方式（开发期）：`launchctl unload + load` 同一个 plist
- Hermes gateway 通过 `~/Library/LaunchAgents/com.jwbot.hermes-webui.plist` 等管理（未确认是否还活跃）
- 实际进程：`pgrep -fl main.py`（AstrBot Python）、`pgrep -fl hermes_cli` （Hermes）

### 升级后已知小问题

- **`dashboard/routes/hall_collaboration.py` 启动报 `'NoneType' object has no attribute 'lower'`**
  - agent roster 初始化失败
  - 不影响主流程，dashboard / 登录 / 三个插件 / Hermes 桥接全部正常
  - 待单独修复

### 验证清单（升级后必跑）

```bash
# 1) AstrBot 版本与 Bootstrap
cd /Users/dianchi/DC-Agent && uv run python -c "from astrbot.core.config.default import VERSION; print(VERSION)"

# 2) Dashboard 登录（验证账户/密码未受影响）
curl -s -X POST http://localhost:6185/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"<USER>","password":"<MD5_HASH>"}' | jq .status

# 3) Hermes gateway 端点
curl -s -o /dev/null -w "HTTP %{http_code}\n" http://localhost:8644/

# 4) Hermes 版本
HERMES_HOME=/Users/dianchi/DC-Agent/hermes-config \
  /Users/dianchi/DC-Agent/hermes-agent/venv/bin/python -m hermes_cli.main --version

# 5) Dreamina CLI 登录态
dreamina user_credit
```

### 后续优化建议（详见 [本地功能契约.md](本地功能契约.md) 末尾）

1. **ruff 配置对齐 upstream**（消除 80% 假冲突）
2. **本地新代码避免侵入 upstream dataclass / 类签名**（避免 git auto-merge 静默丢）
3. **每 2 周同步一次 upstream**（不要积压成大批量合并）
4. **写一个 `scripts/upgrade_smoke.py`** 在 merge 后秒级发现 import / bootstrap 错误
